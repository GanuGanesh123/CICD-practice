name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: print message
        run:  echo "printing sum of two numbers"
      - name: Run Tests
        run: python3 test.py

  
